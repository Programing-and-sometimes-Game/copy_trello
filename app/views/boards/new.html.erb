<%= form_for(@board, url: user_boards_path(@user.id)) do |f| %>
	<%= f.label :board_name %>
	<%= f.text_field :board_name %>
	<%= f.label :board_image, "ボード背景画像" %></p>
	<%= f.attachment_field  :board_image %>
		<%= f.submit "入力" %>
<% end %>